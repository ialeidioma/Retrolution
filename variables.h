#ifndef VARIABLES_H
#define VARIABLES_H // These are include guards they check if it has been defined earlier and prevent double declaration of identifiers such as types enumerators and static variables
#include "string"

int i;
std::string player_name;

#endif
